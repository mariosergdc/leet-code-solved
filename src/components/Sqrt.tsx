import React from "react";

const Sqrt = () => {
  /**
   * @param {number} x
   * @return {number}
   */
  var mySqrt = function (x) {
    return Math.floor(Math.sqrt(x));
  };
  return <div>Sqrt</div>;
};

export default Sqrt;
